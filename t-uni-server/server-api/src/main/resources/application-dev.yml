server:
  port: 10457

logging:
  level:
    t.uni.server: info
    root: info
  pattern:
    dateformat: HH:mm:ss:SSS
  file:
    path: "logs/${spring.application.name}"
    name: "logs/${spring.application.name}"

t.uni:
  master:
    host: 192.168.2.111
    port: 3306
    database: wxy
    username: root
    password: zxxyp

  redis:
    host: localhost
    port: 6379
    database: 0
