ma_open_id 微信小程序openid  多个小程序公用一张用户表，可能需要单独表记录

union_id  同主体下该id一样，登录需要使用

im相关的后续有需要单独存一张表，因为考虑到实际上用户多平台多设备有好多id和Token

邀请时间去除，从后端的新增逻辑上来说，邀请时间就是创建时间

invite_code -->  unique_id  邀请码改为用户唯一id

parent_id  -->  invite_user_id  邀请人id

school_code  -->  auth_school_code  认证学校编码


`view_school_code` varchar(255) DEFAULT NULL COMMENT '用户上次查看的学校编码',

`is_muted` tinyint(1) unsigned zerofill DEFAULT '0' COMMENT '是否被禁言（0否 1是）',  -- 后续记录到社交权限表中
